@layout EmptyLayout
@inject IAuthenticationService AuthenticationService

@code {
    protected override void OnInitialized()
    {
        // Directly navigate to B2C authentication
        AuthenticationService.NavigateToExternalLogin("/", null);
    }
}